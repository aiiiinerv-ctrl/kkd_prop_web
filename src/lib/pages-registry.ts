/**
 * @deprecated Import from `@/lib/pages` instead.
 * Compatibility shim for Home H2/H3 callers during Sprint 4 (#67).
 */
export {
  PAGE_REGISTRY,
  type PageKey,
  type PageRegistryEntry,
} from "@/lib/pages";
